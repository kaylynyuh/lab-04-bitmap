'use strict';

const fs = require('fs');
const buf = fs.readFileSync(`${__dirname}/../../assets/finger-print.bmp`);

//module.exports = function(pathArray, callback) {
  //if (err) return callback(err) {
    //result.push(data.toString())
  //}
//};
